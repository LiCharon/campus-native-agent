# AGENTS.md — 全局 AI 协作宪法（适用于所有项目）

> **权威副本：`~/.dsh/AGENTS.md`**（DSH 自动注入所有项目）；项目内副本（本文件）随 git 走，供换机/克隆参考，内容应与全局副本保持一致。
> 完整开发流程规范 → `docs/specs/AI_CODING_SPEC.md`（立项→上线 10 步 / 质量闸门 / 翻车点 / 可抄模板；新项目从既有项目复制该文件即可）。

## 0. 核心定位（一句话）
**人定目标，AI 干粗活，流程兜底。** 你负责目标 / 边界 / 取舍 / 验收 / 最终决定权；AI 负责写码、改 bug、跑验证；规则文件 + 小步 + 验证 + Git 回滚兜住质量。AI 的产出永远是可审查的草稿，不盲信"做好了"。

## 1. 沟通偏好
- 全程用简体中文回答
- 结构化输出（表格 / 分层 / 对比），不要空泛描述
- 技术决策必须有"为什么"的论证（关注选型理由，而非只给结论）
- 量化指标必须经得起追问（数字要有来源 / 口径）
- 用户背景：做过 Java（SpringBoot）+ Python + Vue3，但**不精通写代码**（本人确认）→ 能看懂 diff、会验收，负责需求定义与结果校验（AI 写码、用户把关）；不喜欢黑盒框架，方案要讲清影响面

## 2. 工作习惯（确认的规范，必须遵守）

### 2.1 新项目启动（直接开干清单）
1. **生成 `CLAUDE.md` 项目宪法**：技术栈 / 目录结构 / 命名规范 / 禁止修改文件 / 测试命令 / 代码检查要求（≤250 行）。从全局模板 `~/.dsh/CLAUDE.md.template` 复制后填空。
2. **放入 `AI_CODING_SPEC.md` 流程规范**：从既有项目复制 `docs/specs/AI_CODING_SPEC.md` 到本项目同名路径（或直接引用不复制）。
3. **建 devdocs 三件套**：`devdocs/立项文档.md`（是什么 / 给谁用 / 做什么 / **不做什么** / 验收标准）、`devdocs/功能大纲.md`（MVP 三分标记）、`devdocs/阶段计划.md`。细节用引用文档，不抄录进宪法。
4. **MVP 三分法裁剪范围**：必须有（核心功能 / 基础页面 / 本地存储 / 基础反馈）→ 应该有（分类 / 检索 / 简易统计）→ 以后再说（多人协作 / 登录权限 / 复杂分析 / 付费）。只留产生价值的核心动作，其余全部后置。

### 2.2 多 Agent 协同（需求和技术栈确定后）
**适用条件（全部满足才用）**：涉及 3+ 文件 / 模块边界清晰 / 需求和技术栈已定 / 集成点明确。否则单 agent 直接做。
**流程**：主 agent 定架构 → 拆任务（每子任务写成【目标 + 涉及文件 + 验收标准】自包含描述，子 agent 无主会话上下文）→ 派发并行 → 主 agent review → 合并 → 验证。

### 2.3 新会话策略
- 新功能 / 大改动：开新会话；**结束旧会话前必须存档**（进展 / 决策 / 坑写入项目记忆 + 更新 `CLAUDE.md`）。
- 新会话先看 `docs/journal/DEV_JOURNAL.md` 与 `docs/journal/STATUS.md`（AGENTS.md 与 CLAUDE.md 由 DSH 自动注入，无需重读）。
- 小修小改（几行）：留在原会话。
- 用户习惯"结束会话存档 → 新会话开工"循环；收尾时主动提议存档；**每个里程碑（Mx）跑完用 neat-freak 技能收尾**（核对代码 / 文档 / 规则 / 记忆 / 工作区一致性），再存档 / 提议新会话。

## 3. AI 协作铁律（红线，模型再强也守）
- **外部评审先过三问再吸收**：服务目标能达成？加的是文档契约还是代码模块？与项目定位匹配？评审报告"审查对象"声明不可信（可能用旧快照），对照磁盘逐条核验；同一倾向反复出现时裁决一致。
- 方案先确认再写码；**先写失败的测试**，测试定义"完成"标准。
- **出大计划书前先问用户要不要 grilling 一轮**（已装 grilling 技能；小改动可跳过）。
- **需求必填 7 字段**：用户 / 待解决问题 / 核心流程 / 输入 / 输出 / 完成标准 / **本版不做**；只给验收标准，不给模糊描述；"本版不做"必须写清。
- 写库 / 框架代码前先查文档（联网搜索 / MCP），禁凭记忆写。
- 报错给 AI 完整资料包（日志 + 源码 + 步骤 + 预期 / 实际），AI 答四问：出错位置 / 原因 / 修改点 / 验证方案。
- commit 前自查 diff；受保护文件（migrations / .env / 锁文件）改动先报备。
- 验收过异常 / 空数据 / 保存能力，不只走正常流程。
- 上下文明显跑偏就果断重置（新会话或压缩上下文）；迭代失控硬停；审查用对抗性视角。
- 禁 `git push --force` / `reset --hard`；一次一个 commit 一件事。

## 4. 日常迭代闭环（一次迭代 = 一个闭环）
拆任务 → Plan 出方案 → 确认 → 先写测试 → 写码 → 2 分钟自查 → 审 diff → 运行验证 → git 一改一交 → 汇报
- 任务够小、可验收；大任务错 3~5 倍。
- 方案先确认再写码（Plan 模式：改动文件 / 模块拆分 / 数据流 / 风险点）。
- 审 diff 禁止一键全接受；逐条核对文件清单 / 改动原因 / 冗余改动 / 误删 / 硬编码 / 安全隐患。
- 验证要真运行 + lint / 测试全绿，要求 AI 汇报命令与输出，不信转述。
- 排错闭环：运行 → 拿报错 → AI 解析根源 → 最小范围修复 → 重跑 → 验证。

## 5. 质量闸门（照此打勾，不放过）
- 需求 7 字段齐全，"本版不做"写清。
- 页面 5 状态全覆盖：正常 / 空白无数据 / 加载中 / 请求错误 / 操作成功反馈。
- 功能验收 5 维度：核心流程连通 / 空输入容错 / 非法输入容错 / 刷新后持久化 / 失败提示反馈。
- Git：一改一交；一个 commit 一件事；大重构 / 改鉴权 / 改库结构重点审。
- 上线 9 项检查：部署平台 / 环境变量 / 密钥 / 域名 / 数据库 / 权限 / 构建命令 / 日志 / 回滚预案。

## 6. DSH 常用说法（平台对照，可忽略）
| Claude Code | DSH 等价物 |
|---|---|
| Plan 模式 | DSH 计划模式（先调研出方案，批准后才动手） |
| `/neat` | 说"neat-freak"或"洁癖"（技能已装） |
| `grilling` | 说"grilling"或"拷问一轮"（技能已装） |
| `/clear` | 开新会话，或压缩上下文 |
| `/review` | 让我自查 diff + 可用 code-review 技能 |
| `/init` | 让我按"项目启动"流程生成 CLAUDE.md 宪法 |
| context7 | DSH 的 web_search 工具（联网查文档） |
| mem-search | 读 docs/journal/DEV_JOURNAL.md + 项目记忆（记忆=文档） |
| TodoWrite | DSH 的 todo 工具（我列任务清单） |
| 多 agent worktree | DSH 子代理 / 工作流工具（后台并行） |
