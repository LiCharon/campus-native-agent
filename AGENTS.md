# AGENTS.md — 全局协作规范（AI 宪法，适用于所有项目）

> 由原 `~/.claude/CLAUDE.md` 迁移而来（2026-08 迁入 DeepSeek Harness）。
> **权威副本：`~/.dsh/AGENTS.md`**（DSH 用户全局指令，自动注入所有项目）；项目内副本随 git 走，供换机/克隆参考。
> 完整开发流程规范 → `docs/AI_CODING_SPEC.md`（立项→上线 10 步/质量闸门/Git 纪律/翻车点/可抄模板）。

## 沟通偏好

- 全程用简体中文回答
- 回答风格：结构化（表格/分层/对比），不要空泛描述
- 技术决策必须有"为什么"的论证，关注选型理由
- 重视量化指标，但数字必须经得起追问
- 用户做过 Java（SpringBoot）+ Python + Vue3 项目，但**不精通写代码**（本人确认 2026-08-04）：能看懂 diff、会验收，负责需求定义与结果校验（AI 写码、用户把关）；不喜欢黑盒框架，方案要讲清影响面

## 工作习惯（用户确认的规范，必须遵守）

### 项目启动与范围（新项目，2026-08-10 补充）

- **新项目起步三件事**：① 生成项目 `CLAUDE.md` 宪法（技术栈/目录/命名/禁止修改文件/测试命令，250 行内）→ ② 复制 `AI_CODING_SPEC.md` 流程规范进项目 docs/ → ③ 建 devdocs 三件套（立项文档/功能大纲/阶段计划），细节用引用文档方式不抄录
- **MVP 三分法（范围裁剪）**：必须有（核心功能/基础页面/本地数据存储/基础反馈）→ 应该有（分类/检索筛选/简易统计）→ 以后再说（多人协作/登录权限/复杂分析/付费体系）；只留产生价值的核心动作，其余全部后置

### 多 agent 协同开发（需求和技术栈确定后）

**适用条件（全部满足才用）：** 涉及 3+ 文件 / 模块边界清晰 / 需求和技术栈已定 / 集成点明确。否则单 agent 直接做。

**流程：** 主 agent 先定架构 → 拆任务（每个子任务写成【目标+涉及文件+验收标准】自包含描述，子 agent 无主会话上下文）→ 派发子 agent 并行 → 主 agent review → 合并 → 验证。

### 新会话策略

- 新功能/大改动：开新会话，但结束旧会话前必须存档（把进展/决策/坑写入项目记忆 + 更新 CLAUDE.md）
- 新会话先看最新 DEV_JOURNAL / STATUS（AGENTS.md 与 CLAUDE.md 由 DSH 自动注入，无需重读）
- 小修小改（几行）：留在原会话
- 用户习惯"结束会话存档 → 新会话开工"循环；任务收尾时主动提议存档；**每个里程碑（Mx）跑完必须用 neat-freak 技能收尾**（核对代码/文档/规则/记忆/工作区一致性），完成后再存档/提议新会话；小收尾可用轻量路径

## AI 协作铁律（红线，模型再强也守）

- **外部评审先过三问再吸收**：服务目标能达成？加的是文档契约还是代码模块？与项目定位匹配？评审报告"审查对象"声明不可信（可能用旧快照），对照磁盘逐条核验；同一倾向反复出现时裁决一致
- 方案先确认再写码；先写失败的测试，测试定义"完成"的标准
- **出大计划书前先问用户要不要 grilling 一轮**（已装 grilling 技能；用户可跳过，小改动不用）
- **需求必填 7 字段**（用户/待解决问题/核心流程/输入/输出/完成标准/**本版不做**）；只给验收标准，不给模糊描述；"本版不做"必须写清
- 写库/框架代码前先查文档（联网搜索/MCP），禁凭记忆写
- 报错给 AI 完整资料包（日志+源码+步骤+预期/实际），AI 答四问：出错位置/原因/修改点/验证方案
- commit 前自查 diff；受保护文件（migrations/.env/锁文件）改动先报备
- 验收过异常/空数据/保存能力，不只走正常流程
- 上下文明显跑偏就果断重置（新会话或压缩上下文）；迭代失控硬停；审查用对抗性视角
- 禁 git push --force / reset --hard；一次一个 commit 一件事

## DSH 常用说法（Claude Code → DSH 对照）

| Claude Code | DSH 等价物 |
|---|---|
| Plan 模式 | DSH 计划模式（先调研出方案，批准后才动手） |
| `/neat` | 说"neat-freak"或"洁癖"（技能已装） |
| `grilling` | 说"grilling"或"拷问一轮"（技能已装） |
| `/clear` | 开新会话，或压缩上下文 |
| `/review` | 让我自查 diff + 可用 code-review 技能 |
| `/init` | 让我按"项目启动"流程生成 CLAUDE.md 宪法 |
| context7 | DSH 的 web_search 工具（联网查文档） |
| mem-search | 读 docs/DEV_JOURNAL.md + 项目记忆（记忆=文档） |
| TodoWrite | DSH 的 todo 工具（我列任务清单） |
| 多 agent worktree | DSH 子代理/工作流工具（后台并行） |
